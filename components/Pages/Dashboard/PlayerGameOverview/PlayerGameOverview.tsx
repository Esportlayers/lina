import { ReactElement } from "react";

export default function PlayerGameOverview(): ReactElement {
    return <div className={'container'}>
        Work in progress

        <style jsx>{`
            .container {
                text-align: center;
                padding: 5rem;
            }    
        `}</style>
    </div>;
}